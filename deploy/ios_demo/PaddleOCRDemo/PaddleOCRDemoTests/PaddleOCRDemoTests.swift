import XCTest
@testable import PaddleOCRDemo

final class PaddleOCRDemoTests: XCTestCase {
    func testAppLaunches() throws {
        // Placeholder test — verifies the test target links correctly
        XCTAssertTrue(true)
    }
}
